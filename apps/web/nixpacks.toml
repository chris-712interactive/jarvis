# Used when Railway Root Directory is set to /apps/web (Nixpacks path).
# Prefer the root Dockerfile + railway.toml (Node 24).

[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
